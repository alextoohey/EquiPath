# Core data pipeline
pandas>=2.0.0
numpy>=1.23.0
openpyxl>=3.1.0
pyarrow>=10.0.0
scikit-learn>=1.3.0

# Web app
streamlit>=1.28.0
plotly>=5.17.0
folium>=0.14.0
streamlit-folium>=0.15.0

# Geographic filtering (zip-code radius search)
pgeocode>=0.5.0
certifi>=2025.0.0

# AI explanations and chat (optional at runtime — the app degrades gracefully
# without an ANTHROPIC_API_KEY)
anthropic>=0.39.0
python-dotenv>=1.0.0

# Optional: voice input/output for the chat assistant
elevenlabs>=2.23.0

# Notebooks (EDA only — not needed to run the app):
#   jupyter, ipykernel, matplotlib, seaborn
